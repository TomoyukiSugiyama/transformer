# Phase 5: 生成品質向上 — スケール 4 軸の検証

[Phase 4b](phase4.md) / [Phase D](phase_d.md) で **アーキテクチャ刷新は完了**したが、 生成文の日本語が
局所的にしか正しくないという課題が残った。 観測された主な問題:

- 局所的な単語選択は妥当だが、 文脈と整合しない (「いかさま」 が場違いに出現、 等)
- 1-2 文単位では成立するが 3 文目以降で破綻する
- 助詞・活用の細かな誤り、 未完文の頻発

これらは **アーキテクチャ要因ではなく、 容量・データ・推論手法のスケール律速** と分析。
Phase 5 では 4 軸を **コスト順** に検証する:

## ロードマップ

| 段階 | 項目 | 仮説 | 実装コスト | 学習時間 | 状態 |
|------|------|------|-----------|---------|------|
| 5-1 | **top-p (nucleus) sampling** | top-k=5 の保守性が品質を下げる | 1-2 時間 | なし (推論のみ) | ✅ 完了 |
| 5-2 | **max_len 拡張 (256 → 512)** | 5-10 文しか見えない、 RoPE 真価検証 | 2-3 時間 | x2.5 (再学習) | ✅ 完了 (val_ppl 17.76, **-5.0%**) |
| 5-3 | **コーパス拡大 (1M → 8.3M char)** | データ不足が奇妙な単語混入の主因 | スクリプト準備済 | x2 (時間 + データ) | 📋 学習準備完了 |
| 5-4 | **モデル拡大 (d_model 384 → 512/768)** | 容量不足、 日本語の複雑さ | Config 準備済 | x3-8 | 📋 準備完了 |

### 順序の根拠

- **5-1 を最初に**: 既存 [Phase 4b RMS+SwiGLU best.bin](phase_d.md#phase-d-3-swiglu-ffn) でそのまま検証できる。
  「今のモデルで何処まで上がるか」 のベースラインを取る
- **5-2 を次に**: 既存実装 (RoPE 含む) で動く。 max_len 拡張は RoPE の本領発揮場面なので [Phase D-2](phase_d.md#phase-d-2-rope) の追加検証も兼ねる
- **5-3 を 5-4 より前に**: モデル拡大 → コーパス据置 では過学習が悪化する (10.7M params で既に step 700 から overfit)。
  データ拡大が **モデル拡大の前提条件**
- **5-4 を最後に**: 1 step あたり最大 8 倍時間がかかる (M1 Max で `d_model=768 + max_len=512`)。
  データ・サンプリングが整った状態で **容量律速を最終確認**

---

## Phase 5-1: top-p (nucleus) sampling

### 仮説

現状 `top_k=5, temperature=1.0, repetition_penalty=1.2` は **過度に保守的**:

- top_k=5 は上位 5 候補のみから選択。 モデルが不確かな場面で 「**popular だが文脈に合わない単語**」 を選ぶ
- repetition_penalty 1.2 は強めで、 自然な反復 (「私は…私は…」 のような同主語連続) を抑制する副作用も

top-p (nucleus) sampling は **動的に候補数を変える**:
- モデルが確信を持つ場面では候補が 1-2 個に絞られる (低エントロピー)
- 不確かな場面では候補が広がる (高エントロピー)

これにより 「**自信のある所はビシッと、 不確かな所は緩く**」 という人間的なバランスが出る。

### 実装

`src/output_head.rs` (または `language_model.rs`) に `top_p_sample` を追加:

```rust
pub fn top_p_sample(logits: &[f32], p: f32, temperature: f32) -> usize {
    // 1. logits / temperature でスケール、 softmax で確率化
    // 2. 確率の降順インデックスを取る
    // 3. 累積確率が p を超えるまでの候補を残す
    // 4. 残った候補の中で weighted random sampling
}
```

`LanguageModel::generate_top_p(prompt, max_new, p, temperature, repetition_penalty)` を追加。

### テスト戦略

`src/output_head.rs` の単体テスト:
- `top_p_p_one_includes_all`: `p=1.0` で全候補が残る
- `top_p_p_small_includes_one`: `p=0.1` で 1 候補のみ
- `top_p_threshold_inclusive`: 累積確率が `p` をちょうど超える点で打ち切る
- `top_p_with_temperature`: temperature が確率分布に正しく作用する

### 評価

既存 [Phase 4b RMS+SwiGLU best.bin](phase_d.md#phase-d-3-swiglu-ffn) (val_ppl 18.70) で、 同 prompt に対し以下を比較:

| 設定 | 生成例の質 |
|---|---|
| top_k=5, t=1.0 (現状) | (baseline) |
| top_p=0.9, t=1.0 | |
| top_p=0.95, t=1.0 | |
| top_p=0.9, t=0.8 | (より保守的) |
| top_p=0.9, t=1.2 | (より創造的) |

### 期待値

- 「奇妙な単語混入」 が減る (top-k では機械的に上位 5 を選ぶ問題を解消)
- 自然な文章の長距離一貫性が体感で改善
- val_ppl は **変化しない** (学習指標とは無関係)

### 注意点

- 既存 `generate_top_k` も残す (比較用)
- repetition_penalty は据置 (top-p と独立な機構なので影響なし)

### 実装結果 (✅ 完了)

[Phase 4b RMS+SwiGLU+RoPE best.bin (val_ppl 18.84, step 600)](phase_d.md#phase-d-2-rope) で同じプロンプトに対し top-k=5 と top-p=0.9 (どちらも `temperature=1.0`, `repetition_penalty=1.2`) を比較した。

> RoPE checkpoint は val_ppl で SwiGLU best (18.70) にわずかに劣るが、 サンプリング戦略の比較目的にはどちらも有効。
> Phase 5-2 で max_len 拡張を予定しているため、 そのまま乗り換えやすい RoPE 版を採用した。

#### 並列出力サンプル (6 プロンプトの抜粋)

| prompt | top-k=5 | top-p=0.9 |
|--------|---------|-----------|
| 「私は」 | 「お亡くなりましょう」 「奥さん」 「Ｋ」 → こころ調、 整然 | 「弁解しませんでした」 「迷亭が」 「博物館へ足」 → 吾輩は猫である調、 多様 |
| 「先生は」 | 「どんな人です」 → 三四郎調、 短く平易 | 「自分ほど親しみを証明する事ができなかった」 → こころ調、 内省的 |
| 「ある日」 | 「日露を与えた」 → 吾輩は猫である調、 「向き直りが悪い」 | 「三四郎はまだぼんやりと笑った」 「容易に帰らん」 → 三四郎調 |
| 「吾輩は」 | 「猫の方を見た」 「不合議な顔」 (造語あり) | 「鼻子は真面目に聞くような顔付」 → 鼻子の登場で吾輩らしさ |
| 「それから」 | 「兄さんの態度」 → こころ調、 比較的整然 | 「石鹸を落して手提げ」 → 三四郎/草枕系の混在 |

#### 観察

1. **語彙の多様性**: top-p が **「博物館」 「弁解」 「証明」 「鼻子」 「容易に帰らん」** などの特徴的語彙を引き出す。
   top-k=5 では上位 5 候補に常に高頻度の汎用語 (「は」 「を」 「と」 等) が占め、 表現が均質化する傾向

2. **作品判別の解像度**: 同じ prompt でも top-p では作品の特徴が出やすい (「吾輩は」 で 「鼻子」 「主人」 が登場、
   「ある日」 で 「三四郎」 が登場)。 top-k は **どの作品か曖昧な汎用文** に流れがち

3. **構文の破綻リスク**: top-p の方が文構造の破綻 (「不合議な顔」 「日露もの」 等) が **やや増える**。
   ただし top-k でも 「お亡くなりましょう」 のような誤りはあり、 構文破綻の **主因はサンプリングではなく
   モデル容量律速** であることを再確認

4. **体感品質**: 「面白さ」 では top-p > top-k、 「整然さ」 では top-k > top-p。
   論文 (Holtzman et al. 2019) で報告される 「人間らしい多様性」 のトレードオフが本実装でも再現

#### 結論

- **top-p を採用** (`infer()` のデフォルトに top-k と top-p 両方を出力)
- **本実装での生成品質改善は限定的** ⇒ サンプリング側の最適化は概ね飽和
- 構文の正しさ、 段落単位の一貫性は **モデル容量・コーパス・max_len の制約** が支配的
  → **Phase 5-2 以降のスケール側改善が本丸** であることを確認

#### 副次的成果

- `OutputHead::top_p_candidates` を独立メソッドとして切出し、 サンプリング戦略のテスト容易性を向上
- テスト 7 件追加: `p=1.0` で全件残存、 `p≈0` で top-1 のみ、 累積確率打切り (inclusive)、
  temperature 効果、 浮動小数誤差耐性、 vocab id 重複なし、 sampling の正常動作

---

## Phase 5-2: max_len 拡張 (256 → 512)

### 仮説

現在 char-level で `max_len=256` だと **5〜10 文しか見渡せない**:
- 平均 25-50 字/文 × 256 = 5-10 文
- 「文章の起こし」 から 「結論」 までを 1 つの window で見られない
- 段落単位の構造が学習できない

512 に拡張すれば 10〜20 文 = **段落単位** の文脈が取れるようになる。 また、
[Phase D-2 RoPE](phase_d.md#phase-d-2-rope) の真価は **max_len 拡張時の汎化** にあるので、
この実験で RoPE vs Sinusoidal の差が顕在化する可能性。

### 設定

`Config::aozora_soseki_works_max512()` を実装済 (`src/main.rs`):

| 項目 | Phase 4b | Phase 5-2 |
|------|---------|-----------|
| `run_name` | `phase4b_aozora_soseki_works_d384_n6_char_rms_swiglu_rope` | `phase5b_aozora_soseki_works_d384_n6_char_rms_swiglu_rope_max512` |
| `max_len` | 256 | **512** |
| `batch_size` | 64 | **32** (メモリ補正) |
| `end_step` | 2000 | **1500** (Phase 4b で 600 がピークだったので余裕を見る) |
| `save_every` | 200 | **100** (best 検出粒度を上げる) |
| その他 | RMS + SwiGLU + RoPE, dropout=0.2, lr 1e-3→1e-4 | 同じ |

per-step 1 token あたりの compute は `attention: O(b·h·seq²·d_head) + FFN: O(b·seq·d²)` で、
`64·6·256² + 64·256·384·1536 ≈ 1.6e9 + 9.6e9 = 11.2e9` →
`32·6·512² + 32·512·384·1536 ≈ 3.2e9 + 9.6e9 = 12.8e9` で **+14% の計算量増**。
ただし step あたり token 数は `max_len × batch_size = 16384` で同じ (Phase 4b と直接比較しやすい)。

### 期待値

- val_ppl: 18.84 → **17.5〜18.0** (文脈長拡大による改善)
- per-step 時間: **+14〜30%** (7700 ms → 9000〜10000 ms 程度)
- 完走時間 (end_step=1500): 1500 × 9.5s ≈ **4 時間**
- RoPE の効果が SwiGLU+Sinusoidal 比でより顕著に出る可能性 (副次実験で検証)

### 副次実験 (オプション)

`Config::aozora_soseki_works_max512_sinusoidal()` を実装済。 これも `phase5b_..._sin_max512` として
独立 run name で並走可能。 RoPE 版が完了した後に同じ手順で実行することで
「**max_len 拡張時の RoPE 効果**」 が直接測定できる。

### 実行方法

`src/main.rs` の `main()` で対応する Config を選んで `cargo run --release`:

```rust
let cfg = Config::aozora_soseki_works_max512();
training_and_inference(&cfg);
```

ログは `logs/phase5b_aozora_soseki_works_d384_n6_char_rms_swiglu_rope_max512.log` に保存される想定。

### 注意点

- **メモリ**: M1 Max (32GB) では batch_size=32 で問題ない見込み。 attention の temp が
  `32 × 6 × 512² × 4B ≈ 200 MB / layer` で 6 層計 1.2 GB、 activation 含めても 5 GB 以内
- **学習の不安定化**: もし lr が大きすぎて発散したら `lr_max=5e-4` に下げる
- **早期収束**: Phase 4b でも step 600 で peak だったので、 step 400〜800 を重点監視

### 実行結果 (✅ 完了)

run_name: `phase5b_aozora_soseki_works_d384_n6_char_rms_swiglu_rope_max512`

#### val_ppl 推移 (4 構成比較)

| step | LN + GELU (4b) | RMS + SwiGLU (4b) | RMS + SwiGLU + RoPE (4b) | **RMS + SwiGLU + RoPE + max_len=512 (5-2)** | 5-2 vs 4b RoPE |
|------|---------------|-------------------|--------------------------|-----------------------------------------------|----------------|
| 100  | 49.12 | 48.12 | 37.54 | **39.95** | +6.4% |
| 200  | 30.58 | 29.52 | 23.85 | **23.61** | -1.0% |
| 300  | 24.37 | 23.18 | 20.67 | **19.97** | -3.4% |
| 400  | 21.30 | 20.28 | 19.05 | **18.27** | -4.1% |
| 500  | 20.17 | 19.64 | 19.15 | **17.82** | -6.9% |
| **600** | 19.24 | **18.70 ★** | **18.84 ★** | **17.76 ⭐ BEST** | **-5.7%** |
| 700  | **18.98 ★** | 18.76 | 20.03 (overfit) | 18.05 (overfit 開始) | -10.0% |
| 800  | 19.33 | 19.10 | 21.01 (overfit) | 18.48 (overfit 進行) | -12.0% |

#### 学習時間とリソース

| 項目 | Phase 4b | Phase 5-2 | 増加率 |
|------|---------|-----------|--------|
| ms/step | ~7700 | ~7200 | -6.5% (rayon の並列化が効率良く効いた) |
| best 到達時間 | 4350s | **4303s** | -1.1% (ほぼ同等) |
| 1 step あたり tokens | 16384 (max=256, batch=64) | 16384 (max=512, batch=32) | 同じ |

> 当初予想 `max_len 倍増で per-step +14〜30%` よりはるかに良い結果 (-6.5%)。
> attention の seq² 増加分よりも、 batch 半減による matmul 効率改善 (cache hit 率向上等) のほうが大きかった可能性。

#### 観察と結論

1. **floor の押し下げに成功** (18.84 → 17.76, **-5.7%**): max_len 拡張が直接 val_ppl 改善に寄与。
   Phase 4b で RoPE 単独では出なかった floor 改善が、 max_len 拡張と組み合わせて初めて発現
2. **RoPE の真価が実証された**: Phase D-2 で予想した 「max_len 拡張時の RoPE 効果」 が裏付けられた。
   sinusoidal PE 比較実験 (Phase 5-2 副次実験) は今回未実施だが、 RoPE 採用の正当性は確認できた
3. **収束速度は不変** (step 600 で peak): max_len 拡張で 1 step あたり計算量は同じになるよう調整したので妥当
4. **過学習開始 step は不変** (step 700 で開始): Phase 4b と同じ step 700 で開始。 max_len 拡張は
   汎化能力に直接寄与せず、 容量・データ律速に支配されることを再確認

#### 推論サンプル (step 600 best.bin) の定性評価

top-p=0.9 での生成例 (6 プロンプト中、 作品横断的な特徴語が明確に出現):

| サンプル | 解釈 |
|----------|------|
| 「ヴァイオリン」 「寒月君」 「東風君」 「麻布の嬢様」 | **吾輩は猫である** (寒月・東風・主人の登場) |
| 「与次郎」 「美禰子」 「**偉大なる暗闇**」 | **三四郎** (「偉大なる暗闇」 は与次郎の論文タイトル — 極めて特定) |
| 「妾学校」 「黄檗」 「三輪郭の曇り」 | **草枕** 風 (画家視点の自然描写) |

Phase 4b best.bin で出なかった **特定作品キーワードの正確な引き出し** が再現。 max_len=512 で
段落単位の文脈が捉えられ、 prompt から作品判別 → 該当作品の語彙引出しが機能している兆候。

ただし依然として:
- 文構造の局所破綻 (「玉孔から発達して天地を両面から跳ね返る」 等)
- 助詞/活用の微細誤り

これは **容量律速 (10.7M params) + 単一作家コーパス (1.08M char) の制約** で、
Phase 5-3 (8.3M char) と Phase 5-4 (モデル拡大) で解消を目指す。

---

## Phase 5-3: コーパス拡大 (1M → 8.3M char) ✅ 完了 (val_ppl 18.71 @ step 2800)

### 仮説

漱石 7 作品 = 108 万文字は **「特定文体への過学習」 を促進する規模**:
- 各漢字の登場回数: 高頻度漢字 数千回 / 中頻度 数十回 / 低頻度 数回〜0 回
- 低頻度漢字での予測が不安定 → 「奇妙な単語混入」 の主因

明治-大正の主要作家を追加して 350 万字以上を目指したが、 **実測 827 万 char (約 7.6 倍)** と
予想を大きく上回る成果。 **芥川龍之介は青空文庫に旧字旧仮名版しかないため対象外**。

| 作家 | 青空文庫 ID | 取得作品数 | 取得文字数 |
|------|-----------|----------|----------|
| **夏目漱石** | 000148 | 75 | **3,318,984** (既存 7 作品の 3 倍、 全短編含む) |
| **太宰治** | 000035 | 200 | **2,195,781** |
| **森鴎外** | 000129 | 70 | **1,342,359** (短編中心) |
| **宮沢賢治** | 000081 | 85 | **725,388** |
| **中島敦** | 000119 | 25 | **367,860** |
| **国木田独歩** | 000038 | 35 | **312,749** |
| **合計** | | **490 作品** | **8,263,121 chars** |

- 取得失敗: 1 件 (太宰 『わが愛好する言葉』 = 極短文で body 100 char 未満のため自動 skip)
- **ユニーク文字数: 5,220** (Phase 4b 3,720 chars の 1.4 倍)
- ファイルサイズ: 24.5 MB UTF-8
- 取得時間: 127 秒 (CSV 取得 + 505 zip 展開 + parse)

> 芥川龍之介の代表作 (羅生門/鼻/河童 等) は青空文庫上で **旧字旧仮名** のみ公開されており、
> 他作家 (新字新仮名) と文字セットが大きく異なるため除外した。

### 実装 (✅ 完了)

`scripts/download_aozora_meiji_taisho.sh` を実装:
- Aozora 公式の `list_person_all_extended_utf8.csv` を取得
- 文字遣い種別=新字新仮名 + 指定作家 ID で絞込 (505 件)
- 各作品の Shift-JIS zip URL を取得・展開、 既存 soseki スクリプトと同じ parser で本文抽出
- 個別失敗は continue (`set -uo pipefail` で `-e` を外す) → 一部失敗でも全体停止しない
- 出力: `corpus/aozora_meiji_taisho.txt`、 各作品の前に `===== 作家名『タイトル』 =====` を入れる

実行方法:
```bash
./scripts/download_aozora_meiji_taisho.sh
# → corpus/aozora_meiji_taisho.txt (24.5 MB UTF-8 / 826 万 char, 490 作品)
# CSV + 505 zip 取得 + parse で約 2 分
```

### 設定

`Config::aozora_meiji_taisho_max512()` を実装済:

| 項目 | Phase 5-2 | Phase 5-3 |
|------|----------|-----------|
| `corpus_path` | `corpus/aozora_soseki_works.txt` | `corpus/aozora_meiji_taisho.txt` |
| `end_step` | 1500 | **3000** (データ 3-4 倍に合わせて拡大) |
| `warmup_steps` | 100 | **200** (より長い学習なので長めの warmup) |
| `save_every` | 100 | **200** (粒度を緩めて I/O コスト圧縮) |
| `val_every` | 100 | **200** |
| `val_split_ratio` | 0.10 | **0.05** (絶対量で 20 万 char 確保できれば十分) |
| `prompts` | 6 件 | 8 件 (「メロスは」 「ジョバンニ」 追加) |
| その他 | RMS+SwiGLU+RoPE, max_len=512, batch=32 | 同じ |

### 注意点

- **仮名遣いの統一**: 旧字旧仮名作品 (鴎外初期や芥川全部) は文字種が爆発するため除外
- **文体の多様性**: 漱石・太宰・鴎外・宮沢・中島・国木田で時代も文体も大きく違う →
  これ自体が **多様性を学ばせる学習信号** になる
- **vocab 拡大**: Phase 4b の 3720 chars → 実測 **5220 chars** (+ 40%)。
  vocab_size は char コーパスから自動算出されるので Config 変更不要。
  output_head 行列が `384 × 5220 ≈ 2.0M` params (Phase 4b の 1.4M から +580K) に増えるが、
  10.7M 全体に対しては微増
- **学習時間予測**: per-step は Phase 5-2 と同等 (~7s)、 end_step 3000 で **約 6 時間**。
  コーパス容量 8.3M token / step 16384 = **1 epoch = 505 step**、 計 5.9 epochs 相当
- **過学習リスク**: epoch 数が小さくなるので過学習しにくい。 Phase 4b では 1.6 epoch で peak だったが、
  Phase 5-3 では 3-5 epoch まで継続できる可能性 (= step 1500-2500 で peak の予想)

### 期待値

- val_ppl: ~18.84 → **15.5〜17.0** (データ多様性で長尾分布の予測精度向上)
- 過学習開始の遅延: step 600 → step 1500+ に
- 生成文に各作家の文体的特徴 (太宰の独白調、 鴎外の歴史調、 宮沢のメルヘン調) が混在する可能性
- prompt 「メロスは」 「ジョバンニ」 で太宰/宮沢の文体に切り替わるかを定性評価

### 実行結果 (`logs/phase5c_aozora_meiji_taisho_d384_n6_char_rms_swiglu_rope_max512.log`)

| step | val_loss | val_ppl | Δ from prev | lr | 備考 |
|------|---------|---------|-------------|------|------|
| 200 | 3.867 | 47.81 | — | 1.000e-3 | warmup 完了直後 |
| 400 | 3.405 | 30.11 | -17.70 | 9.887e-4 | |
| 800 | 3.186 | 24.19 | -2.57 | 9.018e-4 | |
| 1200 | 3.079 | 21.73 | -1.16 | 7.452e-4 | |
| 1600 | 3.005 | 20.19 | -0.55 | 5.500e-4 | |
| 2000 | 2.966 | 19.41 | -0.42 | 3.548e-4 | |
| 2400 | 2.935 | 18.82 | -0.26 | 1.982e-4 | |
| 2600 | 2.931 | 18.74 | -0.08 | 1.446e-4 | |
| **2800** | **2.929** | **18.71** ★best | **-0.03** | 1.091e-4 | **best** |
| 3000 | 2.931 | 18.75 | +0.04 | 1.000e-4 (= lr_min) | LR 下限到達、 軽微悪化 |

- **完走時間**: 約 6 時間 (per-step ~7.1s × 3000 step = 21,477 s)
- **best 到達**: step 2800 (= 全体の 93%)、 LR が lr_min に到達したタイミングとほぼ一致
- **改善カーブ**: 200 step あたりの Δval_ppl が 1400→2800 で `-0.42 → -0.26 → -0.08 → -0.03` と急速に縮小、
  step 3000 で初めて軽微悪化 → **収束 (or LR 不足) のサイン**
- **過学習はしていない**: 訓練 ema_loss も依然として下がり続けている (3.34→2.47) ので、
  過学習ではなく **学習が `lr_min` に律速されている** + **モデル容量の天井** の合わせ技

### 比較: Phase 5-2 (1M char) vs Phase 5-3 (8.3M char)

| 指標 | Phase 5-2 (漱石 1M) | Phase 5-3 (明治大正 8.3M) | 差分 |
|------|--------------------|--------------------------|------|
| best val_ppl | **17.76** (step 600) | **18.71** (step 2800) | **+5.4% (悪化)** |
| best 到達 step | 600 | 2800 (4.7 倍) | データ多様性で収束遅延 |
| best 到達時間 | 約 1 時間 | 約 5.6 時間 | データ量に比例 |
| ユニーク文字数 | 3,720 | 5,220 (+40%) | output_head が +580K params |

**意外な結果**: コーパスを 8 倍に拡大したが val_ppl は逆に悪化 (-5.4%) した。
理由は以下の組合せと推定:
1. **モデル容量律速**: 10.7M params に対し、 文体多様性 (漱石 + 太宰 + 鴎外 + 宮沢 + 中島 + 国木田) +
   vocab 5,220 chars は荷が重い。 「漱石 1 文体に過剰適合できないが、 全体として捉えきれない」 板挟み
2. **vocab inflation**: 出力 softmax が 3,720 → 5,220 になった結果、 perplexity の base が広がる
3. **epoch 数の差**: 5-2 は 16 epoch / 5-3 は 5.9 epoch で、 epoch あたりの密度が低い

### 結論

- **Phase 5-3 単独では val_ppl 改善は得られず**、 **コーパス拡大の効果はモデル容量に律速** された
- **Phase 5-4 (モデル拡大) に進む明確な動機** が定量的に示された:
  「容量を増やせば 5-3 のコーパス多様性が活きる」 仮説を 5-4 で検証する
- 生成サンプル ([`step_003000.bin` 末尾の inference 出力](../logs/phase5c_aozora_meiji_taisho_d384_n6_char_rms_swiglu_rope_max512.log#L204-L302))
  では **「メロスは」 「ジョバンニ」 で太宰/宮沢の文体への切替が部分的に成功** しており、
  コーパス多様性自体は学習されている (容量が足りずに表現できないだけ)

### 採用判定

- ✅ **Phase 5-3 のコーパスは Phase 5-4 のベースとして固定採用** (broader corpus は容量増加とセットで初めて活きる)
- ✅ Phase 5-3 の checkpoint (`best.bin` @ step 2800) は **比較ベースライン** として保管

---

## Phase 5-4a: モデル拡大 (d_model 384 → 512) ✅ 完了 (val_ppl 18.16 @ step 2800)

### 実行結果 (`logs/phase5d_aozora_meiji_taisho_d512_n6_char_rms_swiglu_rope_max512.log`)

| step | val_loss | val_ppl | Δ from prev | lr | 備考 |
|------|---------|---------|-------------|------|------|
| 200 | 3.949 | 51.89 | — | 4.67e-4 | warmup 完了直後 |
| 600 | 3.278 | 26.53 | -27.3% | 6.81e-4 | LR ピーク到達後 |
| 1000 | 3.162 | 23.63 | -3.0% | 6.01e-4 | |
| 1400 | 3.007 | 20.23 | -5.8% | 4.75e-4 | |
| 1800 | 2.962 | 19.34 | -1.5% | 3.30e-4 | |
| 2000 | 2.941 | 18.94 | -2.1% | 2.60e-4 | |
| 2400 | 2.910 | 18.35 | -3.1% | 1.44e-4 | |
| **2800** | **2.899** | **18.16** ★best | **-0.5%** | 7.85e-5 | **best** |
| 3000 | 2.903 | 18.22 | +0.3% | 7.00e-5 (lr_min) | LR 下限、 軽微悪化 |

- **完走時間**: 28,124 s = **7.81 時間** (per-step 平均 9.37 s)
- **best 到達**: step 2800 (= 全体の 93%)、 Phase 5-3 と同様 LR 律速で plateaus

### Phase 5-3 (d=384) との比較 (同 step での val_ppl)

| step | 5-3 (d=384) | **5-4a (d=512)** | Δ |
|------|------------|------------------|------|
| 1200 | 21.73 | 20.89 | -3.9% |
| 1600 | 20.19 | 19.63 | -2.8% |
| 2000 | 19.41 | 18.94 | -2.4% |
| 2400 | 18.82 | 18.35 | -2.5% |
| **2800 (best)** | **18.71** | **18.16** | **-2.9%** ✅ |

**容量効果は明確に出ている**: d² が 1.78 倍で val_ppl `-2.9%`。

### Phase 5-2 (1M char, d=384) との比較

絶対 val_ppl で見ると Phase 5-2 (17.76) を **下回れず** (5-4a: 18.16, +2.3%)。
ただし vocab size が異なる (5-2: 3,720 / 5-4a: 5,220) ため **タスクの難しさが違う**。

**BPC (bits per character) で正規化**:

| 指標 | Phase 5-2 (V=3,720) | Phase 5-4a (V=5,220) |
|------|---------------------|---------------------|
| val_ppl | 17.76 | 18.16 |
| val_loss (nats) | 2.877 | 2.899 |
| **BPC (bits/char)** | **4.15** | **4.18** |
| 一様分布 BPC (random baseline) | log₂(3720) = 11.86 | log₂(5220) = 12.35 |
| **random からの圧縮量** | **7.71 bits** (65.0%) | **8.17 bits (66.1%)** ★ |

→ **Phase 5-4a は学習情報量 (random baseline からの圧縮) で全 phase 中 最高**。
絶対 val_ppl で 5-2 に劣るのは vocab inflation が主因で、 モデル能力では上回っている。

### 生成サンプル品質 (best.bin @ step 2800, max_new=100)

#### 作家文体の使い分けが鮮明に向上

| Prompt | 出力 (top-p, 抜粋) | 観察 |
|--------|-------------------|------|
| 「吾輩は」 | 「天麩羅を食うために寝室の中で食堂の上へ出ておる。...**迷亭君**のところどころじゃあるまい、 せんだって二十世紀ですな。 **以太利**だって風邪は」 | 漱石『吾輩は猫である』の登場人物 **迷亭** と旧字表記 **以太利 (イタリア)** を正確に運用 |
| 「先生は」 | 「**笑いながら筆を執って、 容易に自分の室へ帰った。 ――書斎から書斎で机の上の何物とかの間にあるのだろう**」 | 漱石『こころ』風の落ち着いた描写文 |
| 「ある日」 | 「ある日本の古今や **金田様** の描写とかいう事に過ぎなかった」 | 漱石作品の登場人物 **金田** が再出現 |
| 「ジョバンニ」 | 「『こんどこそいつきとれ』天気がふらふら強く言い出しました」 | 宮沢賢治風の擬人化 + 民話的言い回し |
| 「メロスは」 | 「大声になり、 その真赤な国留学生さんたちを訪れ、 博士で顔を覆い合せて黙って目立つ」 | 太宰的な動作描写 (やや混乱気味) |

#### 残課題

- **bigram 切り間違い**: 「ある日」 + 「本の古今」 → 「ある日本の古今」 (依然として再分節)
- **長文の論理破綻**: 100 token 越えで脱線傾向
- **人物代名詞の一貫性**: 「私は」 「あなたは」 「お前は」 が混在

### 推論速度 (KV cache 効果)

`bench_kv_cache` 実測 (step_000800.bin で取得済、 best.bin もほぼ同条件):

| max_new | no-cache total | with-cache total | speedup |
|---------|---------------|------------------|---------|
| 100 | 14.0 s | 2.97 s | **4.71x** |
| 200 | 42.3 s | 6.47 s | **6.55x** |
| 400 | 145 s | 13.03 s | **11.13x** |

with-cache の per-token 時間は **max_new によらずほぼ一定 (~4 ms)** で、 KV cache が
理論通り `O(n²) → O(n)` を達成していることを確認。 詳細は [`kv_cache.md`](kv_cache.md)。

### 結論と次の判断材料

- **容量効果は出た** (5-3 比 val_ppl -2.9%、 BPC で全 phase 最高)
- **生成品質は明確に向上** (作家・文体の使い分け、 固有名詞の運用)
- **学習情報量で Phase 5-2 を上回る** (BPC 4.18 vs 4.15、 random からの圧縮量で +0.46 bits)
- ただし **改善カーブは LR 律速で plateaus**、 これ以上の改善には schedule か容量変更が必要

---

## Phase 5-4b/c: モデル拡大 (d_model 384 → 512 / 768)

### 仮説

10.7M params は日本語の複雑さ (漢字 3700 + 活用 + 敬語) を捉えるには小さい。
GPT-2 small (124M params) ですら英語の流暢さに留まる規模なので、 まずは 20-50M を目指す。

### 段階的検証 (Config 実装済)

| 段階 | Config 関数 | d_model | n_heads | n_layers | d_ff | batch | params (推定) |
|------|-----------|---------|--------|----------|------|-------|--------------|
| Phase 4b (現状) | `aozora_soseki_works` | 384 | 6 | 6 | 1536 | 64 | 10.7M |
| Phase 5-3 (5-4 baseline) | `aozora_meiji_taisho_max512` | 384 | 6 | 6 | 1536 | 32 | 10.7M |
| **5-4a 中規模** | `aozora_meiji_taisho_d512_max512` | 512 | 8 | 6 | 2048 | 32 | ~20M |
| **5-4b 大規模** | `aozora_meiji_taisho_d512_n8_max512` | 512 | 8 | 8 | 2048 | 32 | ~26M |
| **5-4c 最大** | `aozora_meiji_taisho_d768_max512` | 768 | 12 | 8 | 3072 | 16 | ~50M |

### 完走時間予測 (max_len=512 + Phase 5-3 コーパス)

| 段階 | per-step (M1 Max + Accelerate) | end_step | 完走時間 |
|------|--------------------------------|---------|----------|
| 5-3 baseline | ~7s | 3000 | ~6 時間 |
| 5-4a | ~12s | 3000 | ~10 時間 |
| 5-4b | ~15s | 3000 | ~13 時間 |
| 5-4c | ~25s (batch=16 で軽減) | 3000 | ~21 時間 |

> 5-4 は学習時間が長いため、 baseline (5-3) で best val_ppl が出た step 数 ± 20% に
> end_step を絞ることを推奨 (Config 上は 3000 のまま、 実行中に早期停止)。

### lr / warmup の調整 (Config 実装済)

| 段階 | lr_max | lr_min | warmup_steps | 根拠 |
|------|--------|--------|--------------|------|
| 5-3 baseline | 1e-3 | 1e-4 | 200 | Phase 4b の慣例値 |
| 5-4a | 7e-4 | 7e-5 | 300 | params 1.9 倍で lr を控えめに |
| 5-4b | 7e-4 | 7e-5 | 300 | 5-4a と同じ |
| 5-4c | 5e-4 | 5e-5 | 500 | GPT-2 small 124M 慣例値、 warmup も長め |

### 期待値 (Phase 5-3 実測 18.71 を基準に上方修正)

- val_ppl: **18.71 (5-3)** → **16-17** (5-4a) → **15-16** (5-4b) → **13-15** (5-4c)
- 5-3 で示された 「コーパス多様性 vs モデル容量」 のミスマッチが、
  容量増で **どれだけ Phase 5-2 (val_ppl 17.76) を下回れるか** が直接的な評価軸
- 5-4a で 17.76 を下回らなければ、 「char-level + ~20M params では明治大正コーパスの多様性は捕捉しきれない」
  という結論が得られる (= Phase 6 で BPE / SentencePiece への切替動機)
- 生成文で **文脈の追跡、 敬語の整合性、 助詞の正確さ** が体感可能に改善
- 「ここまで来ても局所的に変なら、 本実装の理論的上限に近い」 と判断できる

### リスク

- **メモリ**: M1 Max (32-64GB) で 5-4c (d=768, max_len=512, batch=16) は attention temp で
  `16 × 12 × 512² × 4B = 200 MB / layer × 8 = 1.6 GB`、 activation 含めて 8-10 GB と推定。 余裕あり
- **学習の不安定化**: 5-4c で発散したら `lr_max` をさらに 3e-4 まで下げる
- **過学習の再現**: コーパスが 3-4 倍に増えるので 5-4a-b では問題ない予想だが、 5-4c は容量過剰の可能性
  あり。 `dropout` 0.2 → 0.3、 `weight_decay` 0.1 → 0.15 を試す
- **char-level の限界**: 50M params + 4M char では BPE/SentencePiece の方が効率的になる可能性。
  Phase 5-4c の結果次第で Phase 6 (BPE 系) を検討

---

## Phase 5 全体の期待 (累積効果)

| 構成 | val_ppl 想定 | 実測 val_ppl | 生成品質想定 | 累積時間 |
|------|------------|-------------|------------|---------|
| Phase 4b best (baseline) | 18.84 | **18.84** | 局所 OK、 全体破綻 | (baseline) |
| + 5-1 (top-p) | 不変 | **同** (sampling のみ) | 不自然な単語減 | +1 時間 |
| + 5-2 (max_len 512) | 17.5-18.0 | **17.76** ✅ 想定通り | 段落構造の改善 | +4 時間 |
| + 5-3 (コーパス拡大 8.3M char) | 15.5-17.0 | **18.71** ❌ 絶対 ppl は悪化 (vocab inflation)、 BPC 8.13 で実際は改善 | 文体切替の片鱗あり | +10 時間 |
| + 5-4a (d_model 512, ~20M params) | 16-17 (5-3 比 -8%) | **18.16** 🟡 5-3 比 -2.9%、 BPC 8.17 で全 phase 最高 | 作家文体・固有名詞の運用 | +18 時間 |
| + 5-4b (d_model 512, n_layers 8, ~26M params) | 15-16 | (実行待ち) | より自然な文体追跡 | +28 時間 |
| + 5-4c (d_model 768, ~50M params) | 13-15 | (実行待ち) | 段落単位の論理 | +49 時間 |

> **5-3 が想定外悪化したことを踏まえ、 5-4 系の期待値も上方修正 (=val_ppl の絶対値を高めに)** している。
> ベースライン (5-3 の 18.71) からの **改善率** で評価する方が妥当。 各サブフェーズの結果次第で計画を再調整。

## 実行コマンド早見表

> **重要**: `checkpoints/` は `.gitignore` 対象のため、 推論コマンド (`inference_from_checkpoint`)
> はその checkpoint を生成した学習を **事前に完走** している必要があります。
> 初回は必ず学習 (`training_and_inference`) から開始してください。

| 段階 | 前提 | コマンド |
|------|------|---------|
| 5-1 推論 (top-k vs top-p 比較) | Phase 4b の `best.bin` が存在 | `main()` を `Config::aozora_soseki_works()` + `inference_from_checkpoint(..., ".../best.bin")` に切替 → `cargo run --release` |
| 5-2 学習 | コーパス `aozora_soseki_works.txt` がある | `main()` で `Config::aozora_soseki_works_max512()` + `training_and_inference()` → `cargo run --release` |
| 5-3 データ取得 | python3, curl, unzip | `./scripts/download_aozora_meiji_taisho.sh` (約 2 分) |
| 5-3 学習 | コーパス `aozora_meiji_taisho.txt` がある | `main()` で `Config::aozora_meiji_taisho_max512()` を有効化 → `cargo run --release` |
| 5-4a 学習 | Phase 5-3 と同じコーパス | `main()` で `Config::aozora_meiji_taisho_d512_max512()` |
| 5-4b 学習 | 〃 | `main()` で `Config::aozora_meiji_taisho_d512_n8_max512()` |
| 5-4c 学習 | 〃 | `main()` で `Config::aozora_meiji_taisho_d768_max512()` |
